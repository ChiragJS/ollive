[project]
name = "inference-sdk"
version = "0.1.0"
description = "Lightweight LLM inference wrapper with telemetry emission"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
  "nats-py>=2.7.0"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
