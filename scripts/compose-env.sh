#!/usr/bin/env bash
set -euo pipefail

# Source this script if you only want the environment in your current shell:
#   source scripts/compose-env.sh
# Then run:
#   docker compose up --build

printf 'Use scripts/compose-up.sh for the full interactive launcher.\n'
printf 'This helper is intentionally a stub because a child shell cannot export variables back to its parent.\n'
